# File Manager

This project is a command-line interface (CLI) file manager built using Node.js. It allows users to perform basic file operations, navigate the file system, retrieve operating system information, calculate file hashes, and compress or decompress files.
